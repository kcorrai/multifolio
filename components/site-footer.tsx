// Paylaşılan alt bilgi — Solar Pop footer'ına devredildi (solar/site-chrome).
// Tek footer: landing, araç sayfaları, pricing, rehberler ve yasal sayfalar.
import { SolarFooter } from "@/components/solar/site-chrome";

export async function SiteFooter() {
  return <SolarFooter />;
}
