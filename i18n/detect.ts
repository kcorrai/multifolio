// Global-only: tek UI dili İngilizce. (TR desteği kaldırıldı — deep global-only geçişi.)
export type Locale = "en";
export const locales: readonly Locale[] = ["en"] as const;
export const defaultLocale: Locale = "en";
